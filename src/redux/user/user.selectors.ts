export const selectUser = (state: any) => state.user;

export const selectToken = (state: any) => state.user.token;

export const selectUserId = (state: any) => state.user.userId;

export const selectTooltipToggle = (state: any) => state.user.tooltipToggle;

export const selectDateExpires = (state: any) => state.user.date_expires;

export const selectBoughtRanks = (state: any) => state.user.boughtRanks;
